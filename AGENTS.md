# 작업 규칙

## 먼저 읽을 문서

- 게임 방향과 확정·미정 사항: [docs/GAME_DESIGN.md](docs/GAME_DESIGN.md)
- 현재 상태, 개발 순서, 검증과 다음 작업: [docs/PROGRESS.md](docs/PROGRESS.md)
- 기존 흐름 참고 자료: [References/prototype.html](References/prototype.html). 사용자 지시와 충돌하면 사용자 지시를 우선한다.

## 진행 방식

- 전체 목표를 유지하되 한 번에 한 단계만 진행한다. 사용자가 결과를 확인하고 “다음 단계”라고 하면 다음 단계로 넘어간다.
- 각 구현 단계는 실행 가능한 작은 결과물로 만든다. 현재 승인 범위는 PROGRESS.md에서 확인한다.
- 사용자는 Unity 초보다. C# 코드는 에이전트가 작성하고, 사용자가 해야 할 조작은 메뉴 이름, 버튼, 대상 파일, 기대 결과까지 구체적으로 안내한다.
- 단계 안의 사소한 구현 선택은 자율적으로 처리한다. 게임 규칙을 바꾸는 중요한 선택은 해당 기능 단계에서 간단한 선택지를 설명하고 사용자와 결정한다.
- 미정 기획을 임의로 확정하지 않는다. 테스트 수치는 임시값임을 표시하고 Inspector 또는 별도 설정 데이터 등에서 쉽게 변경할 수 있게 한다.
- 가능한 컴파일·실행 검증과 오류 수정을 마친 뒤 확인 방법을 안내한다. 직접 확인한 결과와 미검증 사항을 분리하고, 확인하지 않은 것을 성공했다고 기록하지 않는다.
- CLI 및 에디터 연결이 부족하면 필요한 설정을 안내하고, 승인된 범위의 가능한 파일 작업은 진행한다.
- 기존 파일과 지침을 먼저 읽고 사용자의 변경을 보존한다. 요청 범위를 넘어 패키지 설치, 기능 구현 또는 프로젝트 설정 변경을 하지 않는다.
- 단계 종료 시 기획 결정은 GAME_DESIGN.md, 진행·검증·다음 작업은 PROGRESS.md에 갱신한다. 동일 내용을 여러 문서에 복제하지 않고 링크로 참조한다.
- 기술적 이유로 개발 순서 변경이 필요하면 이유를 설명하고 제안한다.
